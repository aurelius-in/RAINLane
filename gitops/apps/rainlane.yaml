apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: { name: rainlane, namespace: argocd }
spec:
  project: default
  source: { repoURL: https://github.com/OWNER/REPO, targetRevision: HEAD, path: gitops/overlays/dev }
  destination: { server: https://kubernetes.default.svc, namespace: rainlane }
  syncPolicy: { automated: { prune: true, selfHeal: true } }


