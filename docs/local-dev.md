# Local Dev Tips

- Use `scripts/unix/dev.sh` or `scripts/windows/dev.ps1` for reload
- If PowerShell quoting fails, prefer `scripts/windows/*.ps1` helpers
- Run `make fmt` before committing for clean diffs
